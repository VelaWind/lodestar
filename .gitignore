node_modules
dist
dist-ssr
.vercel
*.local
.DS_Store
.vite
*.log

# QA evidence: regenerated by `npx playwright test` and `npm test`, never
# committed — the audio is a few hundred kB a run and reproducible from source.
qa-audio/
qa-screenshots/
test-results/
playwright-report/
